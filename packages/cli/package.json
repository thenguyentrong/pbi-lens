{
  "name": "pbi-lens",
  "version": "0.2.0",
  "description": "CLI to screenshot, query and publish Power BI reports — built for AI agent loops",
  "main": "dist/index.js",
  "bin": {
    "pbi-lens": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@pbi-lens/core": "0.2.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
